<UserControl x:Class="Risk.views.UCListGames"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Risk.views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500"
             Name="uc"
             Foreground="White">
    <Border
          CornerRadius="15"
          BorderThickness="4"
          BorderBrush="Transparent"
          SnapsToDevicePixels="True"
        x:Name="borderTauler"
        Cursor="Hand">
        <Border.Background>
            <ImageBrush ImageSource="../Assets/tauler_fusta_partides.png"
                  Stretch="UniformToFill"/>
        </Border.Background>
        <Grid>
        
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
        

            <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" HorizontalAlignment="Center">
                <TextBlock Text="Nom Partida: " FontSize="15"  FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding ElementName=uc, Path=MyPartida.Nom, FallbackValue=GameOne}" TextWrapping="Wrap" FontSize="15"  FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Grid.Row="1" HorizontalAlignment="Center">
                <TextBlock Text="Jugadors: " FontSize="15"  FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding ElementName=uc, Path=MyPartida.currentPlayers, FallbackValue=7}" TextWrapping="Wrap" FontSize="15"  FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Text="/" FontSize="15"  FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding ElementName=uc, Path=MyPartida.maxPlayers, FallbackValue=7}" TextWrapping="Wrap" FontSize="15"  FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </StackPanel>

            <Image Source="../Assets/Riskgame_logo.png" Stretch="Fill" Margin="25" Height="35" Grid.RowSpan="2" Grid.Column="3" />

        </Grid>
    </Border>
</UserControl>
